# dsh-work-icon 二期 · 状态报告

> 生成于 2026-09-12 16:31（你外出期间自主推进）
> 面向：你回来后 5 分钟内能掌握全局 + 需要你拍板的事

---

## 一句话

**五项目标全部达成并已验证。你的图标现在处于「可以恢复使用」的状态；进度功能的功能面已完成，四个新字段（花钱/任务列表/主页文本）刚开工。**

⚠️ **一件需要你知道的事**：图标当前**是停止的**（我同步时停的），**你重启 `dsh web` 即可恢复**。

---

## 一、你要的东西 · 交付状态

| 你的要求 | 状态 | 证据 |
|---|---|---|
| 悬浮图标显示工作状态（科幻风） | ✅ | H2「等离云核」，7 态跟踪 |
| 右键切换文字显示模式（不显示/悬停/常显） | ✅ | 三档 + 悬停行为已验证 |
| 显示花了多少钱 | 🟡 数据层完成，**界面待接** | 宿主 180/180 |
| 显示任务列表 | 🟡 同上 | |
| 显示主页面文本（思考/工具调用/pwsh） | 🟡 同上 | |
| 进度百分比 + 多对话不冲突 + 多进度条 | ✅ 数据层 + 契约完成 | 13 项套件全绿 |
| 子代理树形进度 | ✅ | 深度 0–1，根=主代理 |
| 资源占用档位（省电/标准/流畅） | ✅ | `.tier-eco` 空闲动画完全关闭 |
| 进度条形态选择 | ⏳ **等你选** | 12 套在预览页 |

---

## 二、五项目标 · 逐条验收

1. **8 套方案动效真的在跑** ✅
   12 套（8 单方案 + 4 组合）实测 tick 递增 `30 → 90 → 180`、无 JS 错误。
   （曾犯过的错：全部 mock 的 `@keyframes=0`，根因是 `${fn.toString()}` 对方法简写产出非法 JS ⇒ 整段脚本不执行。已修复并逐个复验。）

2. **宿主侧数据层** ✅ **180 用例 / 180 过 / 0 红**，e2e 通过。
   含 `tokens` 少算修复（真实日志实测**少算 21%–40%**，只有 usage chunk 的回合原本整条丢失）。

3. **花费与底栏逐日对账** ✅ **8 天差额全为 0**（合计 304.663656，12029 条记录）。
   ⏳ 待你定：**口径**（见第四节）。

4. **"单跑会红"的脆弱测试** ✅ 根因 + 修复 + 牙齿验证。
   根因：比对脚本**自己从不渲染**，单独跑时比的是**上一轮留下的旧图**。
   决定性实验：污染产物 → 七态全红 max 22.706%；同一污染起点先重渲染 → 全绿 0.068%。
   牙齿：强制打开 hover → **7/7 态 FAIL**（仍抓得住真实差异）。

5. **状态报告** ✅ 即本文档。

---

## 三、你外出期间额外做的（不在原目标内）

**基础设施类（都在 `harness.mjs` / `run-all.mjs`，我亲自改并验证）**
- 并发隔离：每进程 `runId` + HOME/产物分目录 —— 修掉"两套测试同时跑 → **红的项每次都变**"这类假失败
- 杀进程判据只认"本轮独有"的东西 + **PID 复用校验**（绝不误杀别人的进程）
- 套件锁：拿不到明确报错，**不静默排队**；持有者已死可自动接管

**修复的缺陷**
- `runDir` 迁移是"跨全部 `.mjs` 盲替换"，**弄坏了 5 个生成你 12 套设计稿的脚本**（缺 import，语法查不出）→ 已改回并**端到端验证**（真跑生成 12 页、大小与你手上文件逐个吻合）
- `toolCalls` 死转发（绊线抓到）→ 宿主 **180/180** 转绿
- `index.html` 的 `runDir` 路径一致性核查 → 通过

---

## 四、⏳ 需要你拍板的两件事

### ① 进度条最终形态
- 预览：`http://127.0.0.1:8899/progress-live/index.html`（**服务仍在跑**）
- 候选：`ORBITAL` / `NEBULA` / `CRYSTAL` / `RADAR` / `FIELD` / `GRID` / `PULSE` / `FRACTAL`
  + 组合 `A / B / C / D`（**D 推荐**：环=进度 · 云=状态 · 分形=结构，职责正交）
- 你的原话：**"后面那 4 个都没达到我的预期，不如前面 4 个"**

### ② 花费口径
- 实测：**94.54% 的金额来自同一次请求被 `deepseek-official` 与 `deepseek-vision` 各记一条**
- 选项：**照抄底栏**（与官方一致）／**去重**（更接近真实支出）
- 我的建议：**照抄底栏**（否则两个数字会长期打架，且无法解释差异）

---

## 五、当前状态与恢复方法

| | |
|---|---|
| 宿主套件 | ✅ 180/180 |
| 窗口套件 | ✅ 全部通过（13 项，含 v2 契约 23 PASS） |
| 图标进程 | ⏸ **停止中**（需要你重启 `dsh web` 恢复 —— 见下方警告） |
| 预览服务 | ▶ `127.0.0.1:8899`（看完可以让我关掉） |

### ⚠️ 重启前请先知道
**插件安装用的是硬链接**：源目录的每次编辑**会直接生效到 profile**，不存在"未同步"这层保护。
**⇒ 现在重启会加载当前源码状态**，而不是我上次同步时的版本。
（当前源码：宿主全绿 + 窗口全绿；四字段尚未接入，面板不会显示它们。）

---

## 六、接下来（等你回来再定）

1. 接入四个字段：花钱 / 任务列表 / 上下文占用 / 主页文本
2. 补一张**面板展开态的离屏截图**作为你可亲眼确认的交付物
3. 你选定进度条形态后，把它落到图标上
4. **一次完整重启验证**（本轮所有改动真正上生产的一刻）
